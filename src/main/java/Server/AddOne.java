package Server;

/**
 * Adds one to a number
 */
public class AddOne {
    public static double addOne(double numDouble) {
        return numDouble + 1.0;
    }
    public static int addOne(int numInt) {
        return numInt + 1;
    }
}
